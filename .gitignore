# working artifacts of the wrapper itself
.caplog.*
crash_report_*.txt
crash_core.*.dmp
core
core.*

# leftover of a manual `script` run in the server root
typescript

# unrelated files that must not ride along
mitm_mcp_traffic.db

# internal readme-canon working files
docs/
AGENTS.md
